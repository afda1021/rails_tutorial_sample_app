<%# (リスト13.48) %>
<% if @feed_items.any? %>
  <ol class="microposts">
    <%= render @feed_items %> <%# @feed_itemsの各要素がMicropostクラスを持つため、Micropostのパーシャルを呼び出すことができる(microposts/_micropost.html.erb) %>
  </ol>
  <%= will_paginate @feed_items %>
<% end %>